use crate::ast::source;


/*use combine::{
    Parser,
    Stream,
    parser::{
        byte,
    },
};


pub fn int<Input>() -> impl Parser<Input, Output = u32>
where
    Input: Stream,
{
    combine::many1(byte::digit())
        .flat_map(|digits: Vec<u8>| {
            let mut place: u8 = 1;
            while Some(c) = digits.pop() {

            }
        })
}*/
